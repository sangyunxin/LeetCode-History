#include <iostream>

using namespace std;

int a, b, d;

int main()
{
    cin >> a >> b >> d;
    while(a != 0 || b != 0 || d != 0){
        for(int xy = 0)
        cin >> a >> b >> d;
    }
    cout << "Hello world!" << endl;
    return 0;
}
